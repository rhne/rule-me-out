using UnityEngine;
using System.Collections;

public class QuestionGenerator : MonoBehaviour {
	//generate 2 integers
	//within 0-9, random
	//is it ok if it's twin numbers? _
	//save it to challenger


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
